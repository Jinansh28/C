#include<stdio.h>
#include<limits.h>
int main(){
    int n;
    printf("Enter index");
    scanf("%d",&n);
    char a[40];
    gets(a);
    puts(a);
    /*int i=0;
    while(a[i]!='\0'){
        i++;
    }
    for(int k=i+1;k>n;k--){
        a[i+1]=a[i];
    }
    a[n]='a';
*/
}